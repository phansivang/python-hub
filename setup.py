from setuptools import setup

setup(
    name='cb_pr_calculation',
    version='0.1.0',
    description='A brief description of your package',
    author='Marco',
    packages=['cb_pr_calculation'],
    install_requires=[
        # List your package dependencies here
    ],
)
